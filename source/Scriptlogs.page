Menu="Dashboard:0"
Icon="scriptlogs.png"
---
<?php
require_once '/usr/local/emhttp/plugins/dynamix/include/Helpers.php';
require_once '/usr/local/emhttp/plugins/scriptlogs/scriptlogs_common.php';

$cfg = scriptlogs_read_cfg('scriptlogs');
$scriptlogs_theme_light = scriptlogs_theme_is_light();

$scriptlogs_text = [
    'title' => scriptlogs_t('Script Logs'),
    'settings' => scriptlogs_t('Scriptlogs Settings'),
    'selectScript' => scriptlogs_t('Select a script to see its log.'),
    'autoScroll' => scriptlogs_t('Auto-scroll'),
    'lastUpdate' => scriptlogs_t('Last Update:'),
    'noScripts' => scriptlogs_t('No scripts selected to display.'),
    'loadError' => scriptlogs_t('Error loading script states. Check browser console (F12) for details.')
];
$scriptlogs_text_js = json_encode($scriptlogs_text, scriptlogs_json_options());
if (!is_string($scriptlogs_text_js)) {
    $scriptlogs_text_js = '{}';
}

$refresh_enabled = ($cfg['REFRESH_ENABLED'] ?? '1') === '1';
$refresh_interval = (int)($cfg['REFRESH_INTERVAL'] ?? 10);
if ($refresh_interval < 1) {
    $refresh_interval = 1;
} elseif ($refresh_interval > 86400) {
    $refresh_interval = 86400;
}
$refresh_interval_ms = $refresh_interval * 1000;

$allowed_font_sizes = scriptlogs_allowed_font_sizes();
$log_font_size = $cfg['LOG_FONT_SIZE'] ?? '1rem';
if (!in_array($log_font_size, $allowed_font_sizes, true)) {
    $log_font_size = '1rem';
}
$log_font_size = htmlspecialchars($log_font_size, ENT_QUOTES, 'UTF-8');
$tile_title = htmlspecialchars($scriptlogs_text['title'], ENT_QUOTES, 'UTF-8');
$settings_title = htmlspecialchars($scriptlogs_text['settings'], ENT_QUOTES, 'UTF-8');
$select_script_text = htmlspecialchars($scriptlogs_text['selectScript'], ENT_QUOTES, 'UTF-8');
$auto_scroll_text = htmlspecialchars($scriptlogs_text['autoScroll'], ENT_QUOTES, 'UTF-8');
$last_update_text = htmlspecialchars($scriptlogs_text['lastUpdate'], ENT_QUOTES, 'UTF-8');

$pluginname = 'scriptlogs';

$mytiles[$pluginname]['column1'] = <<<EOT
<tbody id="db-scriptlogs" title="{$tile_title}">
    <tr>
        <td>
            <i class="fa fa-fw fa-close control tile" onclick="dismiss($(this))" title="Close Tile"></i>
            <span class="tile-header">
                <span class="tile-header-left">
                    <i class="fa fa-terminal fa-lg"></i>
                    <div class="section">
                        <h3 class="tile-header-main">{$tile_title}</h3>
                    </div>
                </span>
                <span class="tile-header-right">
                    <span class="tile-header-right-controls">
                        <a href="/Settings/ScriptlogsSettings" title="{$settings_title}">
                            <i class="fa fa-fw fa-cog control"></i>
                        </a>
                    </span>
                </span>
            </span>
        </td>
    </tr>
    <tr id="scriptlogs-collapsed-row" style="display:none;">
        <td>
            <div id="scriptlogs-chips"></div>
        </td>
    </tr>
    <tr id="scriptlogs-content-row">
        <td>
            <div id="scriptlogs-tab-shell">
                <div id="scriptlogs-tab-scrollbar-shell">
                    <div id="scriptlogs-tab-scrollbar-visual" aria-hidden="true">
                        <div id="scriptlogs-tab-scrollbar-thumb"></div>
                    </div>
                    <div id="scriptlogs-tab-scrollbar" aria-hidden="true">
                        <div id="scriptlogs-tab-scrollbar-spacer"></div>
                    </div>
                </div>
                <div id="scriptlogs-tab-bar" role="tablist" aria-label="{$tile_title}"></div>
                <div id="scriptlogs-tab-selected-join" aria-hidden="true"></div>
            </div>
            <div id="scriptlogs-log-box">
                <pre id="scriptlogs-logs" role="tabpanel">{$select_script_text}</pre>
            </div>
            <div id="scriptlogs-footer">
                <label><input type="checkbox" id="scriptlogs-autoscroll" checked> {$auto_scroll_text}</label>
                <span><span class="scriptlogs-updated-at">{$last_update_text}</span> <span id="scriptlogs-timestamp"></span></span>
            </div>
        </td>
    </tr>
</tbody>
EOT;
?>

<style>
/* Theme: prefer Unraid's semantic variables (webGui 7.1+); fall back to a
   palette picked server-side from $display['theme'] for older versions. */
#db-scriptlogs {
    --scriptlogs-tab-surface: var(--table-header-background-color, <?= $scriptlogs_theme_light ? '#f7f7f7' : '#222' ?>);
    --scriptlogs-log-surface: var(--background-color, <?= $scriptlogs_theme_light ? '#fff' : '#1a1a1a' ?>);
    --scriptlogs-border: var(--border-color, <?= $scriptlogs_theme_light ? '#bbb' : '#555' ?>);
    --scriptlogs-border-soft: var(--border-color, <?= $scriptlogs_theme_light ? '#c8c8c8' : '#444' ?>);
    --scriptlogs-scrollbar-border: var(--border-color, <?= $scriptlogs_theme_light ? '#d6d6d6' : '#2c2c2c' ?>);
    --scriptlogs-text: var(--text-color, <?= $scriptlogs_theme_light ? '#222' : '#e0e0e0' ?>);
    --scriptlogs-strong-text: var(--text-color, <?= $scriptlogs_theme_light ? '#111' : '#eee' ?>);
    --scriptlogs-muted-text: <?= $scriptlogs_theme_light ? '#4b5563' : '#999' ?>;
    --scriptlogs-footer-text: <?= $scriptlogs_theme_light ? '#555' : '#777' ?>;
    --scriptlogs-footer-label: <?= $scriptlogs_theme_light ? '#444' : '#858585' ?>;
    --scriptlogs-running-border: <?= $scriptlogs_theme_light ? '#3f7f46' : '#3a6b3a' ?>;
    --scriptlogs-running-text: <?= $scriptlogs_theme_light ? '#213b25' : '#ccc' ?>;
    --scriptlogs-dot-idle: <?= $scriptlogs_theme_light ? '#888' : '#555' ?>;
    --scriptlogs-scrollbar-thumb: var(--scrollbar-color, <?= $scriptlogs_theme_light ? '#8a8a8a' : '#4a4a4a' ?>);
    --scriptlogs-scrollbar-thumb-hover: <?= $scriptlogs_theme_light ? '#666' : '#676767' ?>;
    --scriptlogs-scrollbar-glow: <?= $scriptlogs_theme_light ? 'rgba(0, 0, 0, 0.1)' : 'rgba(255, 255, 255, 0.06)' ?>;
    --scriptlogs-scrollbar-glow-strong: <?= $scriptlogs_theme_light ? 'rgba(0, 0, 0, 0.22)' : 'rgba(255, 255, 255, 0.16)' ?>;
    --scriptlogs-accent: #fd8830;
    --scriptlogs-edge-fade: <?= $scriptlogs_theme_light ? 'rgba(255, 255, 255, 0.9)' : 'rgba(26, 26, 26, 0.88)' ?>;
}
#scriptlogs-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    padding: 7px 0;
}
.scriptlogs-chip {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    padding: 4px 10px;
    border: 1px solid var(--scriptlogs-border-soft);
    color: var(--scriptlogs-muted-text);
    background: var(--scriptlogs-tab-surface);
    white-space: nowrap;
}
.scriptlogs-chip .sl-dot {
    width: 7.2px;
    height: 7.2px;
    border-radius: 50%;
    flex-shrink: 0;
    background: var(--scriptlogs-dot-idle);
}
.scriptlogs-chip.sl-running {
    border-color: var(--scriptlogs-running-border);
    color: var(--scriptlogs-running-text);
}
.scriptlogs-chip.sl-running .sl-dot {
    background: #4CAF50;
    animation: scriptlogs-running-dot-pulse 2.4s ease-in-out infinite;
}
#scriptlogs-tab-shell {
    position: relative;
    width: 0;
    min-width: 100%;
    max-width: 100%;
    box-sizing: border-box;
    padding-top: 10px;
    --scriptlogs-selected-join-left: 0px;
    --scriptlogs-selected-join-width: 0px;
}
#scriptlogs-tab-shell::before,
#scriptlogs-tab-shell::after {
    content: "";
    position: absolute;
    top: 14px;
    bottom: 0;
    width: 18px;
    opacity: 0;
    pointer-events: none;
    transition: opacity 160ms ease;
    z-index: 5;
}
#scriptlogs-tab-shell::before {
    left: 0;
    background: linear-gradient(90deg, var(--scriptlogs-edge-fade), transparent);
}
#scriptlogs-tab-shell::after {
    right: 0;
    background: linear-gradient(270deg, var(--scriptlogs-edge-fade), transparent);
}
#scriptlogs-tab-shell.has-left-overflow::before,
#scriptlogs-tab-shell.has-right-overflow::after {
    opacity: 1;
}
#scriptlogs-tab-scrollbar-shell {
    position: relative;
    width: 100%;
    height: 4px;
    margin-bottom: 1px;
    opacity: 0;
    transition: opacity 180ms ease;
}
#scriptlogs-tab-shell.has-tab-overflow:hover #scriptlogs-tab-scrollbar-shell,
#scriptlogs-tab-scrollbar-shell.is-dragging {
    opacity: 1;
}
#scriptlogs-tab-scrollbar {
    position: absolute;
    inset: 0;
    width: 100%;
    overflow-x: auto;
    overflow-y: hidden;
    scrollbar-width: none;
    opacity: 0;
    pointer-events: none;
    z-index: 1;
}
#scriptlogs-tab-scrollbar::-webkit-scrollbar {
    display: none;
}
#scriptlogs-tab-scrollbar-visual {
    position: absolute;
    left: 0;
    right: 0;
    top: 1px;
    height: 2px;
    background: var(--scriptlogs-log-surface);
    border: 1px solid var(--scriptlogs-scrollbar-border);
    cursor: pointer;
    touch-action: none;
    z-index: 2;
}
#scriptlogs-tab-scrollbar-thumb {
    position: absolute;
    top: 0;
    left: 0;
    height: 2px;
    min-width: 24px;
    background: var(--scriptlogs-scrollbar-thumb);
    border-radius: 999px;
    overflow: hidden;
    box-shadow: 0 0 2px var(--scriptlogs-scrollbar-glow);
    cursor: grab;
    touch-action: none;
    transition: background 140ms ease, box-shadow 140ms ease;
}
#scriptlogs-tab-scrollbar-thumb:active {
    cursor: grabbing;
}
#scriptlogs-tab-scrollbar-visual:hover #scriptlogs-tab-scrollbar-thumb,
#scriptlogs-tab-scrollbar-shell.is-dragging #scriptlogs-tab-scrollbar-thumb {
    background: var(--scriptlogs-scrollbar-thumb-hover);
    box-shadow: 0 0 5px var(--scriptlogs-scrollbar-glow-strong);
}
#scriptlogs-tab-scrollbar-spacer {
    height: 1px;
}
#scriptlogs-tab-bar {
    display: flex;
    flex-direction: row;
    width: 100%;
    box-sizing: border-box;
    overflow-x: auto;
    overflow-y: hidden;
    scrollbar-width: none;
    border-bottom: 1px solid var(--scriptlogs-border);
}
#scriptlogs-tab-bar::-webkit-scrollbar {
    display: none;
}
.scriptlogs-tab {
    padding: 7px 14px;
    font-size: 14.4px;
    font-weight: bold;
    color: var(--scriptlogs-muted-text);
    cursor: pointer;
    border: 1px solid var(--scriptlogs-border);
    border-bottom: none;
    background: var(--scriptlogs-tab-surface);
    user-select: none;
    white-space: nowrap;
    position: relative;
    flex: 0 0 auto;
}
.scriptlogs-tab + .scriptlogs-tab {
    border-left: none;
}
.scriptlogs-tab-label {
    position: relative;
    z-index: 2;
}
.scriptlogs-tab-running-mark,
.scriptlogs-tab-selected-mark {
    display: none;
    position: absolute;
    pointer-events: none;
}
.scriptlogs-tab.sl-running {
    padding-top: 8px;
}
.scriptlogs-tab.sl-running .scriptlogs-tab-running-mark {
    display: block;
    top: 0;
    left: 14px;
    right: 14px;
    height: 1px;
    background: #4CAF50;
    box-shadow: 0 0 3px rgba(76, 175, 80, 0.18);
    animation: scriptlogs-running-mark-pulse 2.4s ease-in-out infinite;
    -webkit-mask-image:
        linear-gradient(90deg, transparent 0%, #000 18%, #000 82%, transparent 100%),
        linear-gradient(180deg, transparent 0%, #000 18%, #000 82%, transparent 100%);
    -webkit-mask-composite: source-in;
    mask-image:
        linear-gradient(90deg, transparent 0%, #000 18%, #000 82%, transparent 100%),
        linear-gradient(180deg, transparent 0%, #000 18%, #000 82%, transparent 100%);
    mask-composite: intersect;
    z-index: 3;
}
.scriptlogs-tab.sl-running.sl-selected .scriptlogs-tab-running-mark {
    height: 3px;
}
.scriptlogs-tab.sl-selected {
    background: var(--scriptlogs-log-surface);
    color: var(--scriptlogs-strong-text);
}
.scriptlogs-tab.sl-selected .scriptlogs-tab-selected-mark {
    display: block;
    left: 0;
    right: 0;
    top: 0;
    height: 2px;
    background: var(--scriptlogs-accent);
    z-index: 1;
}
#scriptlogs-tab-selected-join {
    display: none;
    position: absolute;
    left: var(--scriptlogs-selected-join-left);
    bottom: -1px;
    width: var(--scriptlogs-selected-join-width);
    height: 2px;
    background: var(--scriptlogs-log-surface);
    pointer-events: none;
    z-index: 4;
}
#scriptlogs-tab-shell.has-selected-join #scriptlogs-tab-selected-join {
    display: block;
}
@keyframes scriptlogs-running-mark-pulse {
    0%,
    100% {
        opacity: 0.55;
        box-shadow: 0 0 3px rgba(76, 175, 80, 0.18);
    }
    50% {
        opacity: 1;
        box-shadow: 0 0 9px rgba(76, 175, 80, 0.48);
    }
}
@keyframes scriptlogs-running-dot-pulse {
    0%,
    100% {
        opacity: 0.55;
        box-shadow: 0 0 3px rgba(76, 175, 80, 0.18);
    }
    50% {
        opacity: 1;
        box-shadow: 0 0 10px rgba(76, 175, 80, 0.52);
    }
}
#scriptlogs-log-box {
    border: 1px solid var(--scriptlogs-border);
    background: var(--scriptlogs-log-surface);
    height: 200px;
    min-width: min(360px, 100%);
    overflow-y: auto;
    overflow-x: hidden;
}
#scriptlogs-logs {
    margin: 0;
    padding: 8px;
    color: var(--scriptlogs-text);
    font-family: monospace;
    font-size: <?= $log_font_size ?>;
    white-space: pre-wrap;
    overflow-wrap: break-word;
    word-break: break-all;
    width: 100%;
    box-sizing: border-box;
    border: none;
    background: transparent;
}
#scriptlogs-footer {
    margin-top: 5px;
    font-size: 10px;
    color: var(--scriptlogs-footer-text);
    display: flex;
    justify-content: space-between;
    align-items: center;
}
#scriptlogs-footer label {
    display: flex;
    align-items: center;
    gap: 5px;
    cursor: pointer;
    color: var(--scriptlogs-footer-label);
}
#scriptlogs-footer input[type="checkbox"] {
    width: 11px;
    height: 11px;
    margin: 0;
    accent-color: var(--scriptlogs-accent);
}
</style>

<script type="text/javascript">
    /* global jQuery */
    (function($) {
    var scriptlogsText = <?=$scriptlogs_text_js?>;
    var refreshEnabled = <?=json_encode($refresh_enabled)?>;
    var refreshInterval = <?=json_encode($refresh_interval_ms)?>;
    var scriptlogsRequest = null;

    function scriptlogs_text(key, fallback) {
        return scriptlogsText && scriptlogsText[key] ? scriptlogsText[key] : fallback;
    }

    function scriptlogs_build_chips(scripts) {
        var chips = $('#scriptlogs-chips');
        chips.empty();
        (scripts || []).forEach(function(script) {
            var isRunning = script.status === 'running';
            var chip = $('<div>').addClass('scriptlogs-chip').toggleClass('sl-running', isRunning);
            $('<span>').addClass('sl-dot').appendTo(chip);
            $('<span>').text(script.name).appendTo(chip);
            chips.append(chip);
        });
    }

    function scriptlogs_render_empty_state(message) {
        $('#scriptlogs-tab-bar').empty();
        scriptlogs_build_chips([]);
        $('#scriptlogs-logs').text(message);
        $('#scriptlogs-timestamp').text(new Date().toLocaleTimeString());
        scriptlogs_sync_tab_scrollbar();
        scriptlogs_update_selected_join();
    }

    function scriptlogs_scroll_log_to_bottom(logEl) {
        if (!logEl) return;
        logEl.scrollTop = logEl.scrollHeight;
    }

    function scriptlogs_is_tab_in_view(tabEl, containerEl) {
        if (!tabEl || !containerEl) return false;

        var tabRect = tabEl.getBoundingClientRect();
        var containerRect = containerEl.getBoundingClientRect();
        return tabRect.left >= containerRect.left && tabRect.right <= containerRect.right;
    }

    function scriptlogs_restore_tab_scroll(tabBar, previousScrollLeft, previouslySelected, wasSelectedVisible) {
        tabBar.scrollLeft(previousScrollLeft);

        var selectedTab = tabBar.find('.scriptlogs-tab.sl-selected');
        if (selectedTab.length === 0) return;

        var selectedEl = selectedTab[0];
        if (!selectedEl.scrollIntoView) return;

        var selectedName = selectedTab.data('script-name');
        const selectionChanged = !previouslySelected || selectedName !== previouslySelected;
        var nowOffScreen = !scriptlogs_is_tab_in_view(selectedEl, tabBar[0]);

        if (selectionChanged || (wasSelectedVisible && nowOffScreen)) {
            selectedEl.scrollIntoView({ block: 'nearest', inline: 'nearest' });
        }
    }

    function scriptlogs_get_tab_scroll_metrics(tabScrollbar) {
        var maxScroll = tabScrollbar.scrollWidth - tabScrollbar.clientWidth;
        var trackWidth = tabScrollbar.clientWidth;
        var thumbWidth = maxScroll > 0
            ? Math.min(Math.max((tabScrollbar.clientWidth / tabScrollbar.scrollWidth) * trackWidth, 24), trackWidth)
            : trackWidth;
        var maxThumbLeft = Math.max(trackWidth - thumbWidth, 0);
        var thumbLeft = maxScroll > 0
            ? (tabScrollbar.scrollLeft / maxScroll) * maxThumbLeft
            : 0;

        return {
            maxScroll: maxScroll,
            trackWidth: trackWidth,
            thumbWidth: thumbWidth,
            maxThumbLeft: maxThumbLeft,
            thumbLeft: thumbLeft
        };
    }

    function scriptlogs_update_tab_edge_fades() {
        var tabShell = document.getElementById('scriptlogs-tab-shell');
        var tabBar = document.getElementById('scriptlogs-tab-bar');
        if (!tabShell || !tabBar) return;

        var maxScroll = tabBar.scrollWidth - tabBar.clientWidth;
        var hasLeftOverflow = tabBar.scrollLeft > 1;
        var hasRightOverflow = maxScroll - tabBar.scrollLeft > 1;

        tabShell.classList.toggle('has-tab-overflow', maxScroll > 1);
        tabShell.classList.toggle('has-left-overflow', hasLeftOverflow);
        tabShell.classList.toggle('has-right-overflow', hasRightOverflow);
    }

    function scriptlogs_update_selected_join() {
        var tabShell = document.getElementById('scriptlogs-tab-shell');
        var tabBar = document.getElementById('scriptlogs-tab-bar');
        if (!tabShell || !tabBar) return;

        var selectedTab = tabBar.querySelector('.scriptlogs-tab.sl-selected');
        if (!selectedTab) {
            tabShell.classList.remove('has-selected-join');
            return;
        }

        var tabRect = selectedTab.getBoundingClientRect();
        var shellRect = tabShell.getBoundingClientRect();
        var left = Math.max(tabRect.left, shellRect.left) - shellRect.left;
        var right = Math.min(tabRect.right, shellRect.right) - shellRect.left;
        var joinInset = 1;
        var width = Math.max(right - left - joinInset * 2, 0);

        tabShell.style.setProperty('--scriptlogs-selected-join-left', (left + joinInset) + 'px');
        tabShell.style.setProperty('--scriptlogs-selected-join-width', width + 'px');
        tabShell.classList.toggle('has-selected-join', width > 0);
    }

    function scriptlogs_sync_tab_scrollbar() {
        var tabBar = document.getElementById('scriptlogs-tab-bar');
        var tabScrollbar = document.getElementById('scriptlogs-tab-scrollbar');
        var tabScrollbarSpacer = document.getElementById('scriptlogs-tab-scrollbar-spacer');
        var tabScrollbarThumb = document.getElementById('scriptlogs-tab-scrollbar-thumb');
        if (!tabBar || !tabScrollbar || !tabScrollbarSpacer || !tabScrollbarThumb) return;

        tabScrollbarSpacer.style.width = tabBar.scrollWidth + 'px';
        tabScrollbar.scrollLeft = tabBar.scrollLeft;

        var metrics = scriptlogs_get_tab_scroll_metrics(tabScrollbar);
        tabScrollbarThumb.style.left = metrics.thumbLeft + 'px';
        tabScrollbarThumb.style.width = metrics.thumbWidth + 'px';

        scriptlogs_update_tab_edge_fades();
        scriptlogs_update_selected_join();
    }

    function scriptlogs_bind_tab_scrollbar() {
        var tabBar = document.getElementById('scriptlogs-tab-bar');
        var tabScrollbar = document.getElementById('scriptlogs-tab-scrollbar');
        var tabScrollbarVisual = document.getElementById('scriptlogs-tab-scrollbar-visual');
        var tabScrollbarShell = document.getElementById('scriptlogs-tab-scrollbar-shell');
        if (!tabBar || !tabScrollbar || !tabScrollbarVisual || !tabScrollbarShell) return;

        var isDraggingThumb = false;
        var dragStartX = 0;
        var dragStartScrollLeft = 0;

        tabScrollbar.addEventListener('scroll', function() {
            tabBar.scrollLeft = tabScrollbar.scrollLeft;
            scriptlogs_sync_tab_scrollbar();
        });

        tabBar.addEventListener('scroll', function() {
            tabScrollbar.scrollLeft = tabBar.scrollLeft;
            scriptlogs_sync_tab_scrollbar();
        });

        tabScrollbarVisual.addEventListener('pointerdown', function(event) {
            var metrics = scriptlogs_get_tab_scroll_metrics(tabScrollbar);
            if (metrics.maxScroll <= 0 || metrics.maxThumbLeft <= 0) return;

            var rect = tabScrollbarVisual.getBoundingClientRect();
            var clickX = event.clientX - rect.left;

            if (clickX < metrics.thumbLeft || clickX > metrics.thumbLeft + metrics.thumbWidth) {
                var centeredLeft = Math.min(
                    Math.max(clickX - metrics.thumbWidth / 2, 0),
                    metrics.maxThumbLeft
                );
                tabScrollbar.scrollLeft = (centeredLeft / metrics.maxThumbLeft) * metrics.maxScroll;
            }

            isDraggingThumb = true;
            tabScrollbarShell.classList.add('is-dragging');
            dragStartX = event.clientX;
            dragStartScrollLeft = tabScrollbar.scrollLeft;
            if (tabScrollbarVisual.setPointerCapture) {
                tabScrollbarVisual.setPointerCapture(event.pointerId);
            }
        });

        tabScrollbarVisual.addEventListener('pointermove', function(event) {
            if (!isDraggingThumb) return;

            var metrics = scriptlogs_get_tab_scroll_metrics(tabScrollbar);
            if (metrics.maxScroll <= 0 || metrics.maxThumbLeft <= 0) return;

            var deltaX = event.clientX - dragStartX;
            tabScrollbar.scrollLeft = dragStartScrollLeft + (deltaX / metrics.maxThumbLeft) * metrics.maxScroll;
        });

        function stopDrag(event) {
            isDraggingThumb = false;
            tabScrollbarShell.classList.remove('is-dragging');
            if (event && tabScrollbarVisual.releasePointerCapture) {
                tabScrollbarVisual.releasePointerCapture(event.pointerId);
            }
        }

        tabScrollbarVisual.addEventListener('pointerup', stopDrag);
        tabScrollbarVisual.addEventListener('pointercancel', stopDrag);
        tabScrollbarVisual.addEventListener('lostpointercapture', stopDrag);
        window.addEventListener('resize', scriptlogs_sync_tab_scrollbar);
    }

    function scriptlogs_find_tab(tabBar, scriptName) {
        return tabBar.find('.scriptlogs-tab').filter(function() {
            return $(this).data('script-name') === scriptName;
        }).first();
    }

    function scriptlogs_tab_id(scriptName) {
        var hash = 0;
        var value = String(scriptName || '');
        for (var index = 0; index < value.length; index++) {
            hash = ((hash << 5) - hash) + value.charCodeAt(index);
            hash |= 0;
        }
        return 'scriptlogs-tab-' + Math.abs(hash);
    }

    function scriptlogs_place_tab(tabBar, tab, index) {
        var tabEl = tab[0];
        var currentEl = tabBar.children('.scriptlogs-tab').get(index);
        if (currentEl === tabEl) return;

        if (currentEl) {
            tab.insertBefore(currentEl);
        } else if (tabEl.parentNode !== tabBar[0] || tabEl !== tabBar.children('.scriptlogs-tab').last()[0]) {
            tabBar[0].appendChild(tabEl);
        }
    }

    function scriptlogs_select_tab(tab, shouldScroll) {
        var selectedTab = $(tab);
        if (selectedTab.length === 0) return;

        var tabBar = $('#scriptlogs-tab-bar');
        var logBox = $('#scriptlogs-log-box');
        var logDisplay = $('#scriptlogs-logs');
        var nextLog = selectedTab.data('log-content') || '';

        tabBar.find('.scriptlogs-tab')
            .removeClass('sl-selected')
            .attr('aria-selected', 'false')
            .attr('tabindex', '-1');

        selectedTab
            .addClass('sl-selected')
            .attr('aria-selected', 'true')
            .attr('tabindex', '0');

        if (logDisplay.text() !== nextLog) {
            logDisplay.text(nextLog);
        }
        logDisplay.attr('aria-labelledby', selectedTab.attr('id') || '');

        scriptlogs_update_selected_join();
        if (shouldScroll && $('#scriptlogs-autoscroll').is(':checked')) {
            scriptlogs_scroll_log_to_bottom(logBox[0]);
        }
    }

    function scriptlogs_update_tabs(scripts) {
        var tabBar = $('#scriptlogs-tab-bar');
        var seen = {};

        (scripts || []).forEach(function(script, index) {
            var scriptName = script.name || '';
            var tab = scriptlogs_find_tab(tabBar, scriptName);
            var isRunning = script.status === 'running';

            if (tab.length === 0) {
                tab = $('<div>')
                    .addClass('scriptlogs-tab')
                    .attr('role', 'tab')
                    .attr('tabindex', '-1')
                    .attr('aria-selected', 'false')
                    .attr('aria-controls', 'scriptlogs-logs');

                $('<span>').addClass('scriptlogs-tab-running-mark').appendTo(tab);
                $('<span>').addClass('scriptlogs-tab-label').appendTo(tab);
                $('<span>').addClass('scriptlogs-tab-selected-mark').appendTo(tab);
            }

            seen[scriptName] = true;
            tab.toggleClass('sl-running', isRunning)
                .data('script-name', scriptName)
                .data('log-content', script.log || '')
                .attr('id', scriptlogs_tab_id(scriptName))
                .attr('aria-controls', 'scriptlogs-logs')
                .attr('aria-label', scriptName);

            var label = tab.find('.scriptlogs-tab-label');
            if (label.text() !== scriptName) {
                label.text(scriptName);
            }

            scriptlogs_place_tab(tabBar, tab, index);
        });

        tabBar.find('.scriptlogs-tab').each(function() {
            var tab = $(this);
            if (!seen[tab.data('script-name')]) {
                tab.remove();
            }
        });
    }

    function scriptlogs_handle_tab_keydown(event) {
        var tabBar = $('#scriptlogs-tab-bar');
        var tabs = tabBar.find('.scriptlogs-tab');
        var currentIndex = tabs.index(this);
        var targetIndex = currentIndex;

        if (event.key === 'ArrowRight' || event.key === 'ArrowDown') {
            targetIndex = currentIndex + 1 >= tabs.length ? 0 : currentIndex + 1;
        } else if (event.key === 'ArrowLeft' || event.key === 'ArrowUp') {
            targetIndex = currentIndex - 1 < 0 ? tabs.length - 1 : currentIndex - 1;
        } else if (event.key === 'Home') {
            targetIndex = 0;
        } else if (event.key === 'End') {
            targetIndex = tabs.length - 1;
        } else if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            scriptlogs_select_tab($(this), true);
            return;
        } else {
            return;
        }

        event.preventDefault();
        tabs.eq(targetIndex).focus();
        scriptlogs_select_tab(tabs.eq(targetIndex), true);
    }

    function scriptlogs_status() {
        if (scriptlogsRequest) {
            return scriptlogsRequest;
        }

        scriptlogsRequest = $.ajax({
            url: '/plugins/scriptlogs/scriptlogs_api.php?action=get_script_states',
            dataType: 'json',
            timeout: Math.max(refreshInterval * 2, 5000)
        }).done(function(scripts) {
            const tabBar     = $('#scriptlogs-tab-bar');
            const logDisplay = $('#scriptlogs-logs');
            const timestamp  = $('#scriptlogs-timestamp');

            const previouslySelected = tabBar.find('.sl-selected').data('script-name');
            const previousScrollLeft = tabBar.scrollLeft();
            const prevSelectedEl = tabBar.find('.scriptlogs-tab.sl-selected')[0];
            const wasSelectedVisible = prevSelectedEl ? scriptlogs_is_tab_in_view(prevSelectedEl, tabBar[0]) : false;
            const previousSelectedLog = logDisplay.text();

            if (!scripts || !Array.isArray(scripts) || scripts.length === 0) {
                scriptlogs_render_empty_state(scriptlogs_text('noScripts', 'No scripts selected to display.'));
                return;
            }

            scriptlogs_update_tabs(scripts);
            scriptlogs_build_chips(scripts);

            var selectedTab = scriptlogs_find_tab(tabBar, previouslySelected);

            if (selectedTab.length > 0) {
                var activeScript = scripts.find(function(s) { return s.name === previouslySelected; });
                var shouldScroll = activeScript && activeScript.log !== previousSelectedLog;
                scriptlogs_select_tab(selectedTab, shouldScroll);
            } else {
                scriptlogs_select_tab(tabBar.find('.scriptlogs-tab').first(), true);
            }

            scriptlogs_restore_tab_scroll(tabBar, previousScrollLeft, previouslySelected, wasSelectedVisible);
            scriptlogs_sync_tab_scrollbar();

            timestamp.text(new Date().toLocaleTimeString());
        }).fail(function(jqXHR, textStatus, errorThrown) {
            console.error("Scriptlogs AJAX Error:", textStatus, errorThrown);
            $('#scriptlogs-logs').text(scriptlogs_text('loadError', 'Error loading script states. Check browser console (F12) for details.'));
        }).always(function() {
            scriptlogsRequest = null;
        });

        return scriptlogsRequest;
    }

    function scriptlogs_poll() {
        scriptlogs_status().always(function() {
            if (refreshEnabled && refreshInterval > 0) {
                setTimeout(scriptlogs_poll, refreshInterval);
            }
        });
    }

    function scriptlogs_sync_collapsed_row() {
        var contentRow = document.getElementById('scriptlogs-content-row');
        var collapsedRow = document.getElementById('scriptlogs-collapsed-row');
        if (!contentRow || !collapsedRow) return;

        var isCollapsed = contentRow.style.display === 'none' || contentRow.hidden;
        collapsedRow.style.display = isCollapsed ? '' : 'none';
    }

    // Watch for Unraid's openClose toggling the content row, and swap in/out chips row
    function scriptlogs_watch_collapse() {
        var contentRow = document.getElementById('scriptlogs-content-row');
        var collapsedRow = document.getElementById('scriptlogs-collapsed-row');
        if (!contentRow || !collapsedRow) return;

        scriptlogs_sync_collapsed_row();

        var observer = new MutationObserver(scriptlogs_sync_collapsed_row);

        observer.observe(contentRow, { attributes: true, attributeFilter: ['style', 'hidden'] });
    }

    $(function() {
        $('#scriptlogs-tab-bar')
            .on('click', '.scriptlogs-tab', function() {
                scriptlogs_select_tab($(this), true);
            })
            .on('keydown', '.scriptlogs-tab', scriptlogs_handle_tab_keydown);

        $('#scriptlogs-autoscroll').on('change', function() {
            if ($(this).is(':checked')) {
                scriptlogs_scroll_log_to_bottom(document.getElementById('scriptlogs-log-box'));
            }
        });

        scriptlogs_bind_tab_scrollbar();
        scriptlogs_watch_collapse();

        if (refreshEnabled && refreshInterval > 0) {
            scriptlogs_poll();
        } else {
            scriptlogs_status();
        }
    });
    })(jQuery);
</script>
